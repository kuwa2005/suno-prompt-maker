# Suno Prompt Maker 引継資料

## プロジェクト概要

Suno AI 向けのプロンプト生成Webアプリ。ブラウザで動く静的Webアプリ（HTML+JS+CSS）。

## アクセス情報

| 項目 | URL |
|------|-----|
| GitHub | https://github.com/kuwa2005/suno-prompt-maker |
| 本番 | https://debugprint.com/suno-prompt-maker/ |

## ファイル構成

```
suno-prompt-maker/
├── index.html          # メインページ
├── app.js              # メインロジック
├── data.js             # カテゴリ・テンプレートデータ
├── styles.css          # Tailwind CSS カスタムCSS
├── ternlight-engine.js # セマンティック埋め込みエンジン
├── tag-index.js        # タグベクトルキャッシュ
├── semantic-search.js  # セマンティック検索
├── auto-setter.js      # %自動算出
├── history-search.js   # 履歴検索
├── test.html           # テストページ
├── test-auto-setter.js # 単体テスト
├── debug.js            # Playwright デバッグスクリプト
├── deploy-ftp.sh       # FTPデプロイスクリプト
├── deploy-ftp.sh.example  # FTP設定テンプレート
└── README.md           # ドキュメント
```

## 決め事・ルール

### 応答
- 応答は日本語で

### デザイン
- 確認ダイアログのボタン順は「はい」→「いいえ」（同意/非同意の順）
- Tailwind CSS CDN版を使用（CLI移行はレイアウト崩壊のため撤回済み）
- ダーク/ライトモード対応

### デプロイ
- FTPデプロイ: `./deploy-ftp.sh`（b45.coreserver.jp）
- デプロイ前に `node --check *.js` で構文チェック実行
- data.js の `enka` と `enkaMusic` プロパティが正しくデプロイされているか確認

### カテゴリ管理
- カテゴリ追加は `data.js` の2箇所（CATEGORIES + PROMPT_DATA）
- `CATEGORIES` の `dataKey` は `PROMPT_DATA` のキーと一致させること
- 現在のカテゴリ数: 38

### AI機能
- ternlight: フォールバックモードで動作（CDN不可のため）
- Chrome Translator API: Chrome 138+ のみ対応、ユーザージェスチャーが必要
- メインプロンプトから反映時はジャンルも含める（`excludeGenre=false`）

## 技術スタック

- HTML5 / CSS3 / JavaScript (vanilla)
- Tailwind CSS (CDN版)
- ternlight (フォールバックモード)
- Chrome Translator API (オプション)

## デプロイ手順

```bash
cd /home/mimocode/workspace/suno-prompt-maker
./deploy-ftp.sh
```

## 注意事項

1. **data.js のデプロイ確認**: `enka` と `enkaMusic` プロパティが正しくデプロイされているか確認
2. **Tailwind CSS**: CDN版を使用。CLI版はレイアウト崩壊の原因となるため使用不可
3. **ternlight**: CDN不可のためフォールバックモードで動作
4. **Playwright**: デバッグ用にセットアップ済み。`node debug.js` でコンソールログ取得可能

## 現在のカテゴリ一覧（38個）

genre, style, mood, tempo, fasttempo, slowtempo, era, 年代80, newsound, summer, winter, xmas, major, minor, sparkle, dark, technique, instrument, piano, wagaku, guitar, bass, drum, rhythm, synth, lfo, motion, space, fx, distortion, noise, structure, density, vocal, chorus, exclude, enka, enkaMusic

## セキュリティチェック結果

**機密情報漏洩チェック:**

| ファイル | 状態 | 備考 |
|---------|------|------|
| 引継資料.md | ✓ 安全 | 機密情報なし |
| README.md | ✓ 安全 | 機密情報なし |
| index.html | ✓ 安全 | 機密情報なし |
| app.js | ✓ 安全 | 機密情報なし |
| data.js | ✓ 安全 | 機密情報なし |
| deploy-ftp.sh | ✓ 安全 | .gitignore 登録済み |

**Git リポジトリの状態:**
- `deploy-ftp.sh` は `.gitignore` に登録されており、リポジトリにはコミットされない
- パスワード等の機密情報はローカルにのみ存在
- GitHub リポジトリに漏洩なし

**セキュリティ上の注意点:**
1. `deploy-ftp.sh` を公開リポジトリにコミットしないこと
2. `deploy.env` も同様に公開しないこと
3. FTP パスワードは定期的に変更することを推奨
