# 回帰差分レポート — 2026-07-10

## 旧 OK → 新 OK

- 日本語: `お洒落なカフェで流れるチルい曲`（フォールバックで style/mood 系キーワード検出）
- 英語: `80s city pop nostalgic synth`（genre / style / mood / era）
- 日本語: `切ない演歌 女性ボーカル`（フォールバックで mood + vocal；演歌カテゴリはセマンティック/WASM 依存）
- 空メイン入力（直接 AI 生成はタグのみ／重み付きはスライダー依存）
- 全スライダー 0% + メイン → タグなしでメインのみ（`sunoGetRawWeightSum() === 0`）
- 38 カテゴリ・タグ総数（data.js 整合）
- E2E: タイトル・説明文・生成ボタン・About・キャッシュ再構築ボタン
- セキュリティ: 追跡ファイルに FTP 資格情報なし、`deploy-ftp.sh` / `deploy.env` は gitignore

## 旧 OK → 新 NG

**なし**（意図した仕様変更・入力拒否の追加もなし）

## セキュリティ変更でユーザーに影響するもの

**なし** — 入力ブロックやプロンプト拒否は追加していない。デプロイ資格情報の Git 除外と監査ドキュメントの更新のみ。

## テスト実行

- `npm test` — Node ユニットテスト
- `npm run test:e2e` — Playwright（プレビューサーバー）
